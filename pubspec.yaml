name: jiwu_app
description: "物品记录 - 购买物品管理与统计应用"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.0.0
  google_fonts: ^6.0.0
  shared_preferences: ^2.0.0
  path_provider: ^2.0.0
  share_plus: ^12.0.2
  file_selector: ^1.1.0
  file_selector_android: ^0.5.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
